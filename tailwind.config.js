module.exports = {
    theme: {
      extend: {
        custom: ['MyCustomFont', 'INTER'], // use 'custom' as the class name
        roboto: ['Roboto', 'sans-serif'],       // for Google Fonts example
      },
     
    },
    content: ["./*.html"],
  }
  